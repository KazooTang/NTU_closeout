<div>
    <% @product.each do |p| %>
    <div class="test">
        <div class="img_p"><%= link_to image_tag(p.picture), p.link %></div>
        <div class="message"><%= simple_format(p.message) %></div>
    </div>
    <% end %>
</div>
