<div class="float-message">
    <%= simple_format(@product.message) %>
</div>

<% @product.attachments.each do |x| %>
    <%= link_to image_tag(x.image_src, :class => 'img_show'), x.ourl %>
<% end %>

